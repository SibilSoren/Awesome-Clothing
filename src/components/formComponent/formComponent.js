import React from "react";
import "./formCompnent.css";

const FormInput = (handleChange, label, ...otherProps) => {
  return (
    <div className="group">
      <input type="text" />
    </div>
  );
};

export default FormInput;
